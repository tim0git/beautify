import {width, height} from '../../../global/styles';

export const styles = {
  type: {
    default: {
      alignItems: 'stretch',
      justifyContent: 'center',
      width: width * 0.8,
      height: height * 0.05,
      borderRadius: 10,
      borderColor: 'black',
      borderWidth: 1,
    },
  },
  textAlign: {
    center: {
      alignSelf: 'center',
    },
  },
};
