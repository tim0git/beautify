import {colors, typography} from '../../../global/styles';

export const styles = {
  container: {
    alignSelf: 'stretch',
  },
  text: {
    ...typography.title,
    color: colors.text.title,
  },
};
