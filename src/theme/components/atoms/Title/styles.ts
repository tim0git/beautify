import {typography} from '../../../global/styles';

export const styles = {
  container: {
    alignSelf: 'stretch',
  },
  textAlign: {
    center: {
      alignSelf: 'center',
      ...typography.Title,
    },
  },
};
