import {storiesOf} from '@storybook/react-native';
import React from 'react';
import Search_Screen from './Search_Screen';

storiesOf('Search_Screen', module).add('Default', () => <Search_Screen />);
