import React from 'react';
import 'react-native';
import 'jest-enzyme';
import {shallow, render, mount} from 'enzyme';
import Spinner from './Spinner';

describe('<Spinner />', () => {
  test.todo('should render a spinner component');
});
