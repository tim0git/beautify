import {connect} from 'react-redux';

import Bookings_Screen from '../components/screens/Bookings/Bookings_Screen';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(Bookings_Screen);
